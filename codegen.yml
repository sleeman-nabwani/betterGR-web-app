require:
  - dotenv/config

schema: ${NUXT_PUBLIC_GRAPHQL_HOST}
documents: null

generates:
  graphql/generated/:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      skipDocumentsValidation: true 